# Copy to .env (gitignored). Docker Compose loads this file.

APP_ENV=development
APP_DEBUG=true

# Host path for local PHP; Compose overrides to /data inside the api container.
DATA_PATH=./data

# Host port for the Slim API in compose.dev.yml (container also listens here).
# API_PORT=27180

# Google OAuth 2 client (authorization code via league/oauth2-google).
# Authorized JavaScript origins: http://localhost:5173
# Authorized redirect URI: http://localhost:5173/api/auth/google/callback
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# GOOGLE_REDIRECT_URI=http://localhost:5173/api/auth/google/callback

# HMAC key for the HttpOnly session cookie. Use a long random string in production.
SESSION_SECRET=change-me-to-a-long-random-string

# Cookie Secure flag. Unset: true when APP_ENV=production. Set false for HTTP localhost.
# SESSION_SECURE=false

# Auth route rate limit (per IP). Testing defaults to a high max so unit tests do not flake.
# AUTH_RATE_LIMIT_MAX=10
# AUTH_RATE_LIMIT_WINDOW=60
